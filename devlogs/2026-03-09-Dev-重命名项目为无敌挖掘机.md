# 2026-03-09-Dev-重命名项目为无敌挖掘机

## 任务背景
用户要求将游戏标题从"挖掘机模拟器"修改为"无敌挖掘机"。

## 变更内容
### 1. 技术规范初始化 (SPEC.md)
- 创建了 `SPEC.md` 初始版本，定义了核心功能、技术架构和模块职责。
- 在规范中将项目名称定为"无敌挖掘机"。

### 2. UI 变更 (index.html)
- 修改了 `<div id="info-panel">` 中的 `<h1>` 标题。
- 修改了 `<div id="start-screen">` 中的 `<h1>` 标题。

### 3. 文档变更 (README.md)
- 更新了项目主标题。

## 验证情况
- 检查了 `index.html` 的结构，确保 ID 和类名未受影响。
- 确认 `SPEC.md` 符合 `AGENTS.md` 的模板规范。

状态: 已完成 ✅
