# 🤖 AI 开发工作流规范 (Dev Only v1.5.1)

本文档规定了本项目内 AI 助手的默认工作模式。

---

## 0. 会话启动规则

- 每次开启新会话时，AI 默认进入 **Dev 模式**。
- 标准开场回复格式：

```text
✅ 已进入开发模式
📄 正在加载规范：AGENTS.md
📋 开始执行工作流 Step 1...
```

- 若用户在消息开头使用 `!quick`，AI 进入轻量流程，仅适用于小范围、低风险修改。**禁止**将 `!quick` 用于架构重构、接口调整、数据模型变化或依赖外部上下文的大改动。

---

## 1. 核心指令

1. **配置优先原则**：执行任何任务前，必须先查阅 `docs/feishu.md`，根据资源状态决定是否通过飞书 MCP 读取项目文档、API 文档和测试用例。
2. **上下文来源优先级**：
   - `docs/feishu.md` 中已配置的飞书文档 / API / 测试用例
   - `docs/` 下的 API 文档、设计文档和其他实现细节文档
   - `SPEC.md`
   - 源码、`CHANGELOG.md`、`devlogs/` 最新 5 份日志
3. **禁止臆造外部契约**：若飞书文档、接口文档或测试用例缺失，必须明确指出缺口，不得伪造字段、流程或验收口径。
4. **SPEC 定位原则**：`SPEC.md` 只用于记录项目整体结构、项目定位、技术栈、编程语言、框架选型、模块边界和全局约束，不承载 API 字段、接口细节、详细设计方案等细粒度文档。
5. **文档归档原则**：API 文档、设计文档、接口约定、数据结构细节、流程细节等内容应放在 `docs/` 目录维护。
6. **SPEC 首次初始化原则**：若 `SPEC.md` 不存在或仍为空模板，必须先执行下文的「SPEC 自动初始化流程」，再开始编码。
7. **日志记录原则**：所有实质开发活动都必须在 `devlogs/` 下记录。开始工作前先建草稿，完成后回填状态、验证结果与变更文件。
8. **阻塞处理原则**：若 `docs/feishu.md` 标记为 `已配置` 但飞书 MCP 不可用或读取失败，必须明确说明失败原因，并将当前任务标记为“上下文阻塞”或请求用户补充资料，不得假装已读取成功。

---

## 2. SPEC.md 定位与模板规范

当 AI 需要创建或重构 `SPEC.md` 时，必须保持在“项目级基础规范”范围内，严格包含以下章节：

1. **项目定位与背景**：项目目标、使用场景、当前边界。
2. **技术栈与运行环境**：语言、框架、关键依赖、运行方式。
3. **项目结构与模块边界**：目录结构、模块职责、服务边界。
4. **全局约束与编码约定**：通用规范、跨模块约束、基础约定。
5. **集成与依赖概览**：外部系统、数据库、中间件、核心依赖概况。
6. **非功能性需求**：性能、安全性、稳定性、兼容性。
7. **变更记录**：版本历史与关联日志。

详细 API 文档、详细设计文档、接口字段定义、流程说明等内容应单独维护在 `docs/` 目录。

---

## 3. 开发日志规范 (devlogs)

文件名格式：`YYYY-MM-DD-dev-任务名.md`

每个日志文件必须以以下 YAML frontmatter 开头：

```yaml
---
date: YYYY-MM-DD
role: dev
task: 任务简述
related_task_id:
files_changed:
  - src/xxx.py
  - SPEC.md
status: draft | in_progress | done | blocked
---
```

日志要求：

1. 开始实质开发前创建日志草稿。
2. 完成后回填 `files_changed`、验证结果和最终 `status`。
3. 若交付内容已具备验证条件，在日志末尾注明 `状态: 可测试 ✅`。
4. 获取历史上下文时，最多读取 `devlogs/` 目录下最新 5 份日志。

---

## 4. 标准工作流

### Step 0 工程状态与 SPEC 初始化检测

- 若找不到 `src/`、`app/`、`server/`、`cmd/`、`package.json`、`pyproject.toml`、`requirements.txt`、`go.mod`、`pom.xml` 等工程入口或配置文件，说明代码工程尚未初始化。
  - **行为**：主动询问用户计划使用的技术栈，例如 `Python`、`Node.js`、`React + Vite` 等，再协助生成基础骨架。
- 若 `SPEC.md` 不存在或仍为空模板，必须执行下方的「SPEC 自动初始化流程」。

### SPEC 自动初始化流程

1. 列出项目根目录文件，识别工程配置、源码目录、服务目录、测试目录、`docs/` 目录和关键文档。
2. 若存在代码目录或配置文件，读取入口文件、目录树、依赖信息，以及关键服务端资产，例如控制器、服务层、模型、数据库迁移、消息队列任务、中间件或定时任务。
3. 先查阅 `docs/feishu.md`，确认以下资源的链接和状态：
   - 项目文档
   - API 接口规范
   - 测试用例库
4. 对于 `docs/feishu.md` 中状态为 `已配置` 的资源，必须优先通过飞书 MCP 读取。
5. 若 `docs/feishu.md` 中所需资源未配置，必须请求用户先补充配置；若已配置但飞书 MCP 读取失败，必须立即说明阻塞原因，不得自行编造需求和测试口径。
6. 同步阅读 `docs/` 下相关 API 文档、设计文档，`CHANGELOG.md` 和 `devlogs/` 最新 5 份日志。
7. 严格按本规范第 2 节的 7 个章节生成 `SPEC.md`。
8. 所有内容必须基于扫描到的真实信息；无法确认的部分必须明确写为“待确认”或“暂无”。

### Step 1 读取上下文

1. 先查阅 `docs/feishu.md`。
2. 对于状态为 `已配置` 的资源，必须优先通过飞书 MCP 读取项目文档、接口文档和测试用例。
3. 同步阅读本地：
   - `docs/` 下相关 API 文档、设计文档
   - `SPEC.md`
   - `CHANGELOG.md`
   - `devlogs/` 最新 5 份日志
4. 若所需飞书资源未配置，必须提示用户先更新 `docs/feishu.md`；若已配置但飞书 MCP 不可用或读取失败，必须明确说明原因，并请求用户补齐材料。

### Step 2 创建或更新 Dev 日志

在 `devlogs/` 中创建或更新本次任务日志草稿，记录任务背景、计划修改范围、任务拆分草案和待验证项。

### Step 3 核对 SPEC

编码前先确认 `SPEC.md` 是否覆盖本次任务涉及的整体结构、模块边界、技术约束和服务依赖。若缺的是详细 API、详细设计或数据结构细节，应补充到 `docs/` 目录；若缺的是项目级基础约束，再更新 `SPEC.md`。

### Step 4 列出任务清单

基于飞书文档、当前代码和 `SPEC.md`，先输出本次需求的任务清单，至少包含：

- 任务拆分
- 影响范围
- 风险点
- 验证项
- 预计修改文件

在这一步**不得直接开始编码**。

### Step 5 等待确认

将任务清单提交给用户确认。用户确认前，不得开始实施；若用户调整范围或优先级，必须先更新任务清单和日志，再继续后续步骤。

### Step 6 编写代码

根据已确认的上下文和 `SPEC.md` 执行实现。若用户使用 `!quick`，只允许进行局部、低风险修改。

### Step 7 同步技术规范

若代码改动引入了新的项目级结构、模块边界、语言/框架约束或架构决策，必须同步更新 `SPEC.md`；若改动的是 API、接口字段、详细设计或流程细节，必须同步更新 `docs/` 下对应文档。

### Step 8 验证

完成实现后执行必要验证，至少覆盖：

- 受影响模块的最小可运行检查
- 相关测试或样例验证
- 服务端接口、数据持久化、异步任务或中间件链路的受影响验证（若存在）
- 与飞书测试用例中相关用例的覆盖确认

### Step 9 收尾确认

回填开发日志的 `files_changed`、验证结果与最终 `status`。若当前交付物已具备测试条件，在日志末尾注明 `状态: 可测试 ✅`。
